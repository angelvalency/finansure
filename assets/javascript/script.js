const qna = document.querySelectorAll(".qna")

//click dropdown
qnaElements.forEach (qna=> {
    element.addEventListener("click", () =>
    {
        element.classList.toggle("active");
    });
});